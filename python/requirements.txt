# LEASH - Liveness-Enforced Authorization for Swarm Hierarchies
# Python dependencies

# Cryptographic primitives
cryptography>=41.0.0
ecdsa>=0.18.0

# HD key derivation (BIP-32)
bip32utils>=0.3.0
mnemonic>=0.20

# Utilities
pynacl>=1.5.0

# Testing and simulation
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Network simulation
asyncio-mqtt>=0.16.0  # Optional: for real MQTT testing

# LangChain/LangGraph for AI agents (optional but recommended)
langchain>=0.1.0
langgraph>=0.0.20
langchain-openai>=0.0.5  # If using OpenAI models

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0
networkx>=3.1
rich>=13.0.0  # Terminal formatting and progress bars
